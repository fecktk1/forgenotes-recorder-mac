{
  "name": "forgenotes-recorder-mac",
  "version": "0.6.0",
  "private": true,
  "description": "ForgeNotes macOS recorder for online calls and single-microphone room recordings.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "verify": "node --check renderer/app.js && node --check main.js && node scripts/verify-capture-profile.mjs",
    "prepare:config": "node scripts/write-production-config.mjs",
    "dist:mac": "npm run prepare:config && electron-builder --mac --universal --publish never && codesign --force --sign - release/ForgeNotes-Recorder.dmg && npm run verify:mac",
    "verify:mac": "bash scripts/verify-macos-build.sh release/ForgeNotes-Recorder.dmg"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "io.thecontentforge.forgenotes.recorder.mac",
    "productName": "ForgeNotes Recorder",
    "files": ["main.js", "preload.js", "renderer/**", "icon.png", "config.json", "config.example.json"],
    "directories": { "output": "release" },
    "artifactName": "ForgeNotes-Recorder.${ext}",
    "mac": {
      "target": "dmg",
      "identity": "-",
      "hardenedRuntime": false,
      "notarize": false,
      "strictVerify": true,
      "minimumSystemVersion": "12.0",
      "category": "public.app-category.productivity",
      "icon": "icon.png",
      "extendInfo": {
        "NSMicrophoneUsageDescription": "ForgeNotes records your microphone and the meeting audio you route through BlackHole."
      }
    }
  }
}
